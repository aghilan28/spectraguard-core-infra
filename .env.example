# SpectraGuard Core Infrastructure Environment Configuration
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8000
DEBUG=True

# ------------------------------------------------------------------
# Real CV Engine integration (spectraguard-cv-engine/backend/api/main.py)
# The gateway proxies live camera / inference / tamper / snapshot calls here.
# Run the CV engine backend on its own port (default suggested: 8080).
# ------------------------------------------------------------------
CV_ENGINE_API_URL=http://localhost:8080/api/v1
